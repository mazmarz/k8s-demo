apiVersion: v1
kind: ConfigMap
metadata:
  namespace: metallb-system
  name: config
data:
  config: |
    address-pools:
    - name: default
      protocol: layer2
      addresses:
      - {{ hostvars['worker1'].ansible_host }}-{{ hostvars['worker2'].ansible_host }}
